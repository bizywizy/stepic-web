	server {
		listen 80;	
		server_name ~.;
		location ^~ /uploads/ {
			root /home/box/web;
		}
		
		location ~* \w+\.\w+$ {
			root /home/box/web/public;
		}
		location ~*  ^\w+$ {
			return 404;
		}
	}
